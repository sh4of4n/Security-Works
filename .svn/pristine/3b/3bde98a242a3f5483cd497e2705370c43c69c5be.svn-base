import { paths } from "routers/routes";

const nonAuthRoutes: Array<string> = [
  process.env.REACT_APP_ENV === "LOCAL" && paths.Playground,
  // process.env.REACT_APP_ENV === "LOCAL" && paths.Login,
]

export default nonAuthRoutes;
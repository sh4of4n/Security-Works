import { css } from "@emotion/react";
import { Theme } from "styles/theme/DefaultTheme";

const TaskAssignCss = (theme: Theme) => css`
  // &.task-assignment {
  //   font-family: Arial, Helvetica, sans-serif;
  // }

  // & td{
  //   padding: 0.6rem 1rem;
  // }

  & .arrow-buttons {
    padding: 0.7rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 100%;
    align-items: center;
  }
  
  &.task-assignment .arrow-buttons svg {
    height: 15%;
    cursor: pointer;
  }

  & .table-container {
    max-height: 19rem; 
    overflow-y: auto;
    overflow-x: auto; 
    padding: 0;   
  }

  & .table-container thead th {
    position: sticky;
    top: 0;
    z-index: 1;
    font-size: 0.95rem;
  }
  
`;

export default TaskAssignCss;
